{
  "builds": [
    {
      "src": "main.py",
      "use": "@vercel/python"
    },
    {
      "src": "luxor-hub/package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "echo 'using prebuilt dist - no install needed'",
        "buildCommand": "echo 'using prebuilt dist - no build needed'",
        "outputDirectory": "dist"
      }
    }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/main.py" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
